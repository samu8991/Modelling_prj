v1 - Has no observer, it is the same as hw3, but with the new system
v2 - Implementing observer for the leader system and neighborhood observers for the agents. The caveat is that the system is controlled through the
    observer in a different way.
v3 - Each agent is a stabilized system with with an observer and a pole-placement controller.