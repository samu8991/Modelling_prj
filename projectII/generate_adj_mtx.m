function mtx = generate_adj_mtx(type, n)

if type == 'chain'
    switch(c)
    case 1
        % Chain 1
    case 2
        % Chain 2
    case 3
        % Chain
    end
elseif type == 'tree'
    
elseif type == 'complete'
    
elseif type == 'dicatator'
    
end

end